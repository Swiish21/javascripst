// this is a function that returns the name "John"
function displayName() {
    name = "John";
    return name;
}

console.log(displayName());

// below is a function that adds two numbers, and returns the result.
function math_opps() {
    var a = 5;
    var b = 10;
    var c = a + b;
    return c;
}

console.log(math_opps());